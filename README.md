# 杨文生 / 视觉设计作品集

这是一个无构建步骤的静态作品集网站，基于素材目录中的展板与 PDF 档案制作。首页展示四个完整案例：浮光、光栖、DESK//ON、澜信·艺工鲸；每个案例都有设计思路、图组大图查看和对应档案入口。

本地预览（当前机器已启动）：

```powershell
python -m http.server 8765
```

然后打开 `http://localhost:8765/`。也可以直接双击 `index.html`，但本地 HTTP 服务对 PDF 和图片路径更稳定。
